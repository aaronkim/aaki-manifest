<?xml version="1.0" encoding="UTF-8"?>
<manifest>
    <remote  name="aaki"
             fetch=".." />

    <default revision="develop"
             remote="aaki"
             sync-j="4" />

    <project name="aaki-setenv" path="setenv">
      <linkfile src="aaki" dest="../aaki"/>
      <linkfile src="setenv.in" dest="../aaki-setenv.in"/>
    </project>

</manifest>
